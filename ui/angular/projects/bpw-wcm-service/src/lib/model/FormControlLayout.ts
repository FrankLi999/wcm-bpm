export interface FormControlLayout {
  title?: string;
  hint?: string;
  editable?: boolean;
  expandable?: boolean;
  expanded?: boolean;
  rows?: number;
  displayFlex?: boolean;
  flexDirection?: string;
  flex?: string;
  placeHolder?: string;
  listItems?: number; //Number of items of the array listed on the form
}
