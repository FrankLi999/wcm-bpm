import { EditModeDirective } from "./edit-mode.directive";

describe("EditModeDirective", () => {
  it("should create an instance", () => {
    const directive = new EditModeDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
