import { ContentIdDirective } from './content-id.directive';

describe('ContentIdDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentIdDirective(null);
    expect(directive).toBeTruthy();
  });
});
