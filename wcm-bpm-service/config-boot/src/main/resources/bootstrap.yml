spring:
  cloud:
    config:
      server:
        encrypt:
          enabled: false
encrypt:
  key-store:
    location: "classpath:/jks/config-server.jks"
    password: "Passw0rd"
    alias: "config-server"
    secret: "Passw0rd"
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka